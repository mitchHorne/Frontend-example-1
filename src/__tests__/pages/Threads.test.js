// import { Threads } from '../../pages'
// import { renderAsync, wrapPageElement } from '../../testUtils'
// import * as threadUtils from '../../utils/thread'

describe('Threads', () => {
  // it('renders without crashing', async () => {
  //   jest.spyOn(threadUtils, 'getAdventure').mockResolvedValue([])

  //   await renderAsync(wrapPageElement(Threads))
  // })

  // it('should match snapshot', async () => {
  //   const threads = [
  //     {
  //       id: 'thread1',
  //       name: 'thread name',
  //       description: 'thread description',
  //       nodes: [{ id: '1', text: 'tweet text' }],
  //       twitterProfile: {
  //         profileImageUrl: 'thread.picture',
  //         username: 'user.handle',
  //         name: 'user name'
  //       }
  //     }
  //   ]

  //   jest.spyOn(threadUtils, 'getExampleThreads').mockResolvedValue(threads)

  //   const { asFragment } = await renderAsync(wrapPageElement(Threads))

  //   expect(asFragment()).toMatchSnapshot()
  // })
  it('is a temp test', () => expect(true).toBe(true))
})
